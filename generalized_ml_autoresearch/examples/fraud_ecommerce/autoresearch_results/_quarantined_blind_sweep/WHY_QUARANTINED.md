# Quarantined: Exps 10-44 (blind-sweep batch)

These experiments were generated by `run_full_sweep.py` as a blind hyperparameter
grid sweep without per-experiment diagnosis. This violates the CLAUDE.md
"Research-Driven Experiment Selection (STRICT — no blind sweeps)" rule:

> The experiment loop is NOT a grid search. It is a research process. Every single
> experiment must follow this exact sequence: Diagnose → Cite → Hypothesize → Predict
> → Run ONE → Analyze → Document.

The user explicitly called out this protocol violation. The experiments are preserved
here for audit trail but excluded from the live leaderboard. The replacement strict-
protocol lineage starts at Exp 10 in the live log.

Useful information from the quarantined batch (validated under proper protocol later):
- HP variations within ±50% of Exp 6 baseline gave test AUC in [0.523, 0.541] band
- Multi-seed std on champion config = 0.006 (5 seeds) — very low variance
- Rolling time-windowed velocity features did NOT improve over Exp 6's simpler velocity-only set
- Framework bug: xgboost backbone silently dropped scale_pos_weight (fixed in core/backbones/gbm.py)
